server:
  port: 8080

spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      routes:
        - id: order_route
          uri: http://order-service:8081
          predicates:
            - Path=/orders/**
