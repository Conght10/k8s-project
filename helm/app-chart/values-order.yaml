# Cấu hình riêng cho Order Service
replicaCount: 2

image:
  repository: conght/order-service
  tag: "1.0.0"

service:
  type: ClusterIP
  port: 8081

config:
  appMessage: "Dịch vụ Quản lý Đơn hàng đang sẵn sàng!"
  featureFlag: true
